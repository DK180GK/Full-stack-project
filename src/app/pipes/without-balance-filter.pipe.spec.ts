import { WithoutBalanceFilterPipe } from './without-balance-filter.pipe';

describe('WithoutBalanceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WithoutBalanceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
